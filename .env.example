# 数据库配置
# SQLite 数据库文件路径
# 开发环境使用 file:./dev.db，生产环境可使用 PostgreSQL 等其他数据库
DATABASE_URL="file:./dev.db"

# NextAuth 身份验证配置
# 应用运行的完整 URL
# 开发环境: http://localhost:3000
# 生产环境: https://yourdomain.com
NEXTAUTH_URL="http://localhost:3000"

# NextAuth 加密密钥
# 🚨 极其重要的安全配置 - 必须在生产环境更换！🚨
#
# ⚠️ 安全警告：
# 1. 这是用于加密 JWT 和 session 的密钥，必须保密
# 2. 如果泄露，攻击者可伪造任意用户的登录凭证
# 3. 生产环境必须使用随机生成的强密钥（至少 32 字节）
# 4. 不同环境（开发/测试/生产）必须使用不同的密钥
# 5. 绝不要将真实密钥提交到代码仓库或分享给他人
#
# 📋 生产环境部署步骤：
# 1. 在服务器上执行以下命令生成密钥：
#    openssl rand -base64 32
#
# 2. 将生成的密钥设置到环境变量（不要保存在 .env 文件）：
#    export NEXTAUTH_SECRET="生成的密钥"
#
# 3. 验证密钥已设置：
#    echo $NEXTAUTH_SECRET | wc -c  # 应该 >= 32
#
# 其他生成方法：
# 方法 2: node -e "console.log(require('crypto').randomBytes(32).toString('base64'))"
# 方法 3: npx auth secret
#
# ❌ 危险示例（请勿在生产环境使用）：
NEXTAUTH_SECRET="your-secret-key-replace-this-with-random-string"
#
# ✅ 正确示例（生产环境）：
# NEXTAUTH_SECRET="Kx9v2Jp8Qm4Zr7Wn5Tb3Yc6Vf1Dg0Hs"  # 这只是示例，请生成自己的！
